[pytest]
pythonpath = . ..
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# 배포·추론 테스트는 저장소 루트에 있다. 기본 실행에 넣지 않으면 아무도 돌리지
# 않고, 막으려던 타임아웃 버그가 그대로 돌아온다.
testpaths = tests ../deployment/tests ../inference/tests
